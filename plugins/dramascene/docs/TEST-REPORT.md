# 测试与开发记录 · 1.1.0

模块dramascene 1.1.0；案例《帘外》3.0.0；协议1.1。

## 本地执行结果

`python plugins/dramascene/scripts/validate_scene.py plugins/dramascene/examples/lianwai/scene.json`：ok=true，0 errors，0 warnings，manual_review_required=true。

`python -m unittest discover -s plugins/dramascene/tests -q`：**101 tests，全通过**，其中41项原有兼容回归、60项新协议测试。原始执行输出见[test-output.txt](test-output.txt)。

新增测试包括语境引用与来源分类、背景补句、话轮承接、镜头话轮顺序、全部机位露天、背景建筑角色、隐藏室内文字、旁白分析句式与意图猜测、重复旁白、背景增量、可选旁白、想象授权、效果触发／时态／声音／音乐／回人规则、结构化稿与可读稿一致性，以及部分畸形输入不崩溃。

模块Markdown相对链接逐个检查无断链；10个对白话轮、17个室外镜头、1条背景旁白、2个视听效果、6条原诗字幕均有记录。两个manifest版本一致，入口保留。

## 文字预审

原词与虚构分开：深睡余醉来自原文，饮酒杯数与小园规则等归入adaptation。对白完整连贯，依次承接茶约、饮酒、赏花、风势、花量与共同停留。原词过去不另演；人物起坐与问答是同一当前场景的局部顺序。

所有镜头在露天空地，房屋仅作远景装饰。说话时以人物中近景为主，具象效果位于肩后或自然空隙。旧旁白两句从当前案例删除；新版只叙述小园开放规则和既有人物关系。

## 验证边界

以上是本地标准库测试与文字预审。元数据和字面匹配不能证明语义自然或声画质量，可能漏掉同义改写。没有执行宿主安装、真人听读、配音、音乐生成、视频生成、试拍或试剪；没有验证用户设备。

本地无法直连GitHub，仓库读写通过授权连接器完成。修改前完整模块树与远端一致；提交前将重新读取main，避免覆盖其他更新。提交后核对提交差异与模块树。完整上游仓库未在本地运行全量测试。
