# 测试与开发记录

模块：dramascene 1.0.0；案例：《帘外》2.0.0。

## 执行结果

本地 Python 标准库执行 `validate_scene.py`：`ok=true`，0 errors，0 warnings，`manual_review_required=true`。

`python -m unittest discover -s plugins/dramascene/tests -v`：**41 tests，全部通过**。原始输出见 [test-output.txt](test-output.txt)。

测试涵盖：有效案例、错误类型／缺字段、过去事件重演、单一当前场景、一般剧本结构继承、正常动作／情绪／视线、残酒与推断语义声明、古语台词／旁白、原诗误作口语、原诗字幕删字、引用与角色错误、重复编号、说话者不可见、物件承接对白、旁白抢声、文字堆叠、慢动作低落表演指令、物件长镜提醒、时长类型错误、人工复核标志、反应镜回人、非诗词稿无需诗句字幕、JSON 解析错误、插件结构以及可读稿／结构化稿一致性。

两个旧 marketplace 的完整原始字节已对照 GitHub blob SHA 核验：

- `.claude-plugin/marketplace.json`：`d7d4385ba41810b7a5840c9f75b98f7a1bd0c91d`。
- `.agents/plugins/marketplace.json`：`d4be1308388d69766e5d2e052ff68d6c3bbfda15`。

修改后逐项比较：旧插件条目、名称、owner／interface 与其他元数据不变，仅各追加一个 `dramascene` 条目。模块内部 Markdown 相对链接已检查；不把上游模块未在本地复制误判为被删除。

## 文字预审

对白全部重新表述；“稀稀落落／睡得那么沉”分别保留雨的稀疏与睡眠深度；“我估计／应该”保留推断。正常起身、走两步、转身与卷帘不再被禁止；固定斜倚、永久悬手、失神下视不作为新稿动作。说话镜头以人物中近景为主，海棠只作短切补充。

## 边界

这些结果只证明文件结构、明确字面规则与测试行为符合本检查器。语义声明不是作者本意的自动证明，镜头和表演质量不能由标签证明。没有执行宿主应用安装、真人朗读、浏览器演示、拍摄、AI 配音或视频生成；没有在用户设备上运行测试。

本地网络无法直接克隆 GitHub，本次仓库读取与提交使用授权 GitHub 连接器；本地测试用新增文件和已读取的登记文件，不宣称对完整上游仓库运行了全量测试。
